package:
  name: terratorch

  benchmark_packages:
    # Local package stored in this repository
    - requirement_specifier: "ado-vllm-performance>=1.13.2"
      experiments:
        - "geospatial-vllm-bench-deployment-custom-dataset"
